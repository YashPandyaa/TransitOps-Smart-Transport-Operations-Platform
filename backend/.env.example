# Server
PORT=3001
CLIENT_ORIGIN=http://localhost:5173

# Postgres
PGHOST=localhost
PGPORT=5432
PGDATABASE=transitops
PGUSER=postgres
PGPASSWORD=postgres

# Auth (change for prod)
JWT_SECRET=dev_secret_change_me
JWT_EXPIRES_IN=7d

