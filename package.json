{
  "name": "transitops",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "start": "concurrently -k --names \"BACKEND,FRONTEND,BROWSER\" --prefix-colors \"cyan,magenta,green\" \"npm --prefix backend start\" \"npm --prefix frontend run dev\" \"node scripts/open-browser.mjs\"",
    "lint": "echo \"(optional) add eslint\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}

